# 下一轮上下文补充：真实构建核验

请把这份文件与 `next_thread_context.md` 一起读取。

## 新增事实

- 在 `output/research-clones/` 中浅克隆并验证了 SDBurt、Astro Kami、hmbldv；依赖和构建完成后已清理临时克隆，只保留报告。
- SDBurt：安装成功，`pnpm build` 成功；`astro check` 为 0 errors / 0 warnings / 0 hints；但当前 Node 24 不符合仓库声明的 `>=22.12.0 <23`。
- Astro Kami：安装成功，`pnpm build` 成功；产出 43 个页面并完成 Pagefind；有 17 个提示，依赖和构建链明显更重。
- hmbldv：依赖安装成功，但 `npm run build` 失败；仓库示例数据与 schema 不一致，`src/content/wiki/` 也不存在；npm audit 报告 18 个漏洞，其中 2 个 critical。

## 更新后的决策

不要把 hmbldv 评为可直接采用的模板；只保留它的项目版本、提交记录、Wiki 和文档式详情页信息模型。

继续以 SDBurt 作为内容结构和 Token 的主要参考，以 Astro Kami 作为文章/Showcase/配置集中化的补充参考。两者都不应被整套复制；下一步要在当前项目分支建立最小内容模型后，再比较迁移成本。

## 证据位置

- 详细命令和原始结果：`docs/research/构建核验记录_候选模板.md`
- 依赖、许可证和采用取舍：`docs/research/深度核验_依赖许可证与采用建议.md`

