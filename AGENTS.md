# Maki Portfolio Site Agent Notes

Keep replies concise.

Before changing portfolio content, read `docs/CONTENT_MAINTENANCE.md`.

This Astro site is static. The user does not write code; they provide content,
images, links, and corrections. Agents should update the content files and code
for them, then run `npm run build`.

