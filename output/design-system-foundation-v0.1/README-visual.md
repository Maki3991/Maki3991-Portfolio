# 视觉判断板入口

推荐打开同目录的 `index-visual.html`。

它把设计系统中需要用户判断的四类参数做成并列候选：

- 色彩：A 冷白索引、B 纸张索引、C 高对比索引
- 字体：A 中性无衬线、B 安静编辑感、C 大标题主张
- 密度：A 宽松、B 平衡、C 紧凑
- 形状：A 克制边界、B 柔和表面、C 硬朗索引

每一轮都有选择按钮和自由文本框；页面底部还可以写混合方案或推翻候选，最后导出 Markdown。选择只保存在当前浏览器，不会改动生产代码。
